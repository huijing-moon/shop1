import React from "react";

const CommonTableRow = ({ children }) => {
    return (
        <tr>
            {
                children
            }
        </tr>
    )
}

export default CommonTableRow;